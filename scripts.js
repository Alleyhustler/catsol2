// Add shaking effect to the body
document.addEventListener('DOMContentLoaded', () => {
    // Add a class to the body to trigger the shaking effect
    document.body.classList.add('shake');
});
